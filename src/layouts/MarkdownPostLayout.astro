---
import BaseLayout from './BaseLayout.astro';
const { frontmatter } = Astro.props;
---
<!-- Start of Selection -->
<BaseLayout pageTitle={frontmatter.title}>
    <div class="py-8 max-w-3xl mx-auto">
        <p class="text-muted-foreground text-sm">{frontmatter.pubDate.toLocaleDateString()}</p>
        <h1 class="text-3xl font-bold mb-2">{frontmatter.title}</h1>
        <p class="text-lg italic mb-4">{frontmatter.description}</p>
        <p class="text-muted-foreground">Written by: <strong>{frontmatter.author}</strong></p>
        <img src={frontmatter.image.url} alt={frontmatter.image.alt} class="w-full max-w-xs rounded-lg shadow-md mb-4" />

        <div class="flex flex-wrap gap-2 my-4">
            {frontmatter.tags.map((tag: string) => (
                <span class="tag bg-muted border border-dotted border-gray-300 rounded-md px-3 py-1 text-sm">
                    <a href={`/tags/${tag}`} class="text-blue-600 hover:underline">{tag}</a>
                </span>
            ))}
        </div>

        <div class="prose prose-lg mt-6">
            <h2>Introduction</h2>
            <p>This article explores the key concepts and insights related to {frontmatter.title}. We will delve into the main topics and provide practical examples to enhance your understanding.</p>

            <h2>Key Takeaways</h2>
            <ul>
                <li>Understanding the fundamentals of {frontmatter.title}.</li>
                <li>Exploring real-world applications and case studies.</li>
                <li>Tips and best practices for implementation.</li>
            </ul>

            <h2>Conclusion</h2>
            <p>In conclusion, {frontmatter.title} is an essential topic for anyone looking to deepen their knowledge and skills in this area. We hope this article provides valuable insights and encourages further exploration.</p>
        </div>
    </div>

    <slot />
</BaseLayout>
<!-- End of Selection -->